pipeline {
    agent any

    stages{

        stage('one') {
            steps {
                sh 'echo Hello'
                }
              }
        stage('two') {
            steps {
                sh 'echo world'
                }
          }

      }

}